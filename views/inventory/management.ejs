<%# Content Heading %>

<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

<% messages() %>

<a href="/addClassification">Add new classification</a>
<a href="/addInventory">Add new inventory</a>

