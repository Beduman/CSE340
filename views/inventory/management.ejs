<%# Content Heading %>

<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

<% messages() %>

<a href="/inv/addClassification">Add new classification</a>
<a href="/inv/addInventory">Add new inventory</a>

